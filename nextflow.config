profiles {
    standard {
        process {
            container = 'quay.io/nextflow/bash'
        }
    }
    
    docker {
        docker {
            enabled = true
            fixOwnership = true
        }

        process {
            container = 'quay.io/nextflow/bash'
        }

    }
}
